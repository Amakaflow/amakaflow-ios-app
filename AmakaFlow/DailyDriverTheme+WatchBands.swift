//
//  DailyDriverTheme+WatchBands.swift
//  AmakaFlow
//
//  AMA-2374 — Watch preview band accents kept out of DailyDriverTheme.swift so
//  that file stays at the SwiftLint baseline file_length (608).
//

import SwiftUI

extension DailyDriver {
    /// Mobility / prep band accent (AMA-2369 send-flow Runna bands, `#8890A0`).
    static let mobilityBand = Color(hex: "8890A0")
}
