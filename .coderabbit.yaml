# CodeRabbit configuration — AmakaFlow iOS
# Docs: https://docs.coderabbit.ai/reference/configuration

language: "en-US"

reviews:
  profile: "assertive"
  # AMA-1592: at least 2 real reviews; pause after 2; a 3rd only via
  # `@coderabbitai review` if many real issues remain. Never on every push.
  auto_review:
    enabled: true
    drafts: false
  auto_incremental_review: true
  auto_pause_after_reviewed_commits: 2
  request_changes_workflow: false
  high_level_summary: true
  # AMA-2439: disable autofix to prevent agent ping-pong (same as amakaflow-docs #74)
  finishing_touches:
    autofix:
      enabled: false
  # AMA: CodeRabbit is for CODE only. Doc-only PRs must not consume CR credits.
  path_filters:
    - "!**/*.md"
    - "!**/*.mdx"
    - "!docs/**"
    - "!**/CHANGELOG*"
    - "!**/LICENSE*"
    - "!**/*.txt"

knowledge_base:
  # AMA: org-wide learnings so a lesson taught on one repo applies everywhere.
  learnings:
    scope: global
  linear:
    usage: enabled
    team_keys:
      - "AMA"
  mcp:
    usage: enabled
  code_guidelines:
    enabled: true
    filePatterns:
      - "CLAUDE.md"
      - "docs/**/*.md"
