name: CodeRabbit Linear Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  coderabbit:
    uses: Amakaflow/.github/.github/workflows/coderabbit-linear-review.yml@main
    secrets:
      CODERABBIT_API_KEY: ${{ secrets.CODERABBIT_API_KEY }}
