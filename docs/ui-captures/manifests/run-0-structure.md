# Capture manifest — run-0-structure

- build: `6a750d5b`
- device: iPhone 17 Pro Max · iOS-26-1
- artifacts: `docs/ui-captures/6a750d5b/run-0-structure/`
- auth: mock identity + fixtures — NOT the seeded staging account

Navigation is by accessibility identifier, not screen coordinates, so a
screen that cannot be reached is recorded BLOCKED rather than
screenshotting whatever happened to be on screen.

| Screen | Status | Evidence |
| --- | --- | --- |
| `library` | CAPTURED | 28 identifiers |
| `profile` | CAPTURED | 28 identifiers |
| `start-sheet` | CAPTURED | 16 identifiers |
| `today` | CAPTURED | 22 identifiers |
| `workout-detail` | CAPTURED | 29 identifiers |

5 of 5 screens captured.
