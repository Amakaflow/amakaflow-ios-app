# AMA-2260 — Generate My Week journey (PR maestro label, warn-only)
#
# AMA-2292: Workouts root tab removed from Daily Driver Proto IA. Schedule /
# Generate Week chrome is deferred (Today is completed-diary only).

appId: com.myamaka.AmakaFlowCompanion
---
- runFlow: ../../../_lib/clerk-signin.yaml
- assertVisible:
    id: "af_tabbar"
- assertVisible:
    id: "today_tab"
- assertVisible:
    id: "library_tab"
- assertVisible:
    id: "profile_tab"
- assertNotVisible:
    id: "workouts_tab"
